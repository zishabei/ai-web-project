:8080 {
    @api path /api/*
    handle @api {
        reverse_proxy host.docker.internal:8000
    }

    handle {
        root * /srv/dist
        try_files {path} /index.html
        file_server
    }
}
